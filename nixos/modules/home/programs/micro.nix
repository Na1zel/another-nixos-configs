{ ... }:

{
  # ============================================================
  # Micro
  # ============================================================

  programs.micro = {
    enable = true;
    settings = {
      colorscheme = "simple";
    };
  };
}
