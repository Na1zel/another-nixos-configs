{ config, pkgs, pkgs-stable, inputs, lib, ... }:

{
  imports = [ ./modules/home ];

  home.stateVersion = "26.05"; # меняй на свою когда будешь ставить первый раз

  # ============================================================
  # GTK (пока не используется, оставлено для памяти)
  # ============================================================

  #gtk.gtk4.theme = null;
}
